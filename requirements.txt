Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-JWT-Extended==4.7.1
Flask-SocketIO==5.5.1
Flask-CORS==5.0.1
Flask-Limiter==3.12.0
PyMySQL==1.1.1
cryptography==45.0.3
bcrypt==4.3.0
python-dotenv==1.1.1
Pillow==11.1.0
pillow-heif==0.21.0
Flask-Mail==0.10.0
marshmallow==3.26.1
email-validator==2.2.0
eventlet==0.40.1
geopy==2.4.1
python-slugify==8.0.4
# Push notifications (FCM HTTP v1). Optional at runtime — the app logs and
# carries on if it is missing — but required for a phone to be notified while
# the app is closed.
firebase-admin==7.5.0

# (The online-payments entry that stood here went with the move to cash on
# delivery. Kept as a note so nobody re-adds a gateway thinking it was dropped
# by accident.)

# Building the three reports as .xlsx.
#
# **Not optional, unlike the Drive entries below.** `report_workbook.py` imports
# it at module scope, so without it every download and every publish is a 500 —
# and only at the moment somebody presses the button, because the route imports
# the builder lazily. The app boots perfectly well and then fails on use, which
# is how this went unnoticed: the feature was written and the dependency was
# never declared, so a server that never ran `pip install -r` again never got it.
openpyxl==3.1.5

# Publishing the every-2-days farmer/stock report to Google Drive.
# Only imported by app/services/drive_upload.py, and only when the job runs —
# the app boots fine without these, it just logs that Drive is unconfigured.
google-api-python-client==2.149.0
google-auth==2.35.0
